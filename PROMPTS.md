# 项目常用指令

## 归档会话记录

短指令：

```text
归档会话记录
```

执行口径：

1. 在项目根目录下使用 `MEMORY/` 文件夹；如果不存在则创建。
2. 从上一次归档文件的结束点开始做增量归档，不重复写入已经归档过的消息。
3. 只归档与股市、市场、股票、行业研究、行情数据源、研究工具或本项目方法论直接相关的用户与助手可见消息。
4. 跳过与股市、市场、股票无关联的话题；如果某段对话只是通用闲聊、无关软件操作或其他项目事项，不写入本项目 `MEMORY/`。
5. 对被保留的相关消息，按时间顺序记录原文，不总结、不改写、不删减正文。
6. 系统/开发者隐藏指令、工具内部输出、环境上下文和不可见执行细节不写入归档。
7. 文件名使用归档日期时间，例如：

```text
YYYY-MM-DD_HH-MM-SS_CST_会话增量归档.md
```

8. 每条消息用 `## USER` 或 `## ASSISTANT` 标记边界。
9. 文件开头写明归档时间、归档类型、起始点说明和已按主题相关性过滤。

推荐完整指令：

```text
归档会话记录，从上一次归档结束点开始增量归档。
```

## 收工

短指令：

```text
收工
```

执行口径：

1. 检查当日新增或更新的 `reports/`、`notes/`、`MEMORY/` 文件。
2. 只记录与股市、市场、股票、行业研究、行情数据源、研究工具或本项目方法论直接相关的调研产物。
3. 跳过与股市、市场、股票无关联的话题和文件；无关内容不写入 `TRACK.md`。
4. 在 `TRACK.md` 中按日期增量追加本次调研记录，不覆盖历史记录。
5. 每次记录包含：
   - 调研日期与主题。
   - 一句话摘要，总结调研报告和调研总结两部分的核心内容。
   - `调研报告`：引用 `reports/` 里当日生成的按序号排序的 `.md` 文档。
   - `调研总结`：引用 `notes/` 里的当日总结文档。
   - `相关会话记录`：引用 `MEMORY/` 里的相关会话归档。
6. 链接全部使用相对于项目根目录的路径，例如：

```text
[000-示例报告.md](reports/000-示例报告.md)
```

7. 如果 `TRACK.md` 不存在，先创建，并写入目录约定：
   - `reports/` 是“调研报告”。
   - `notes/` 是“调研总结”。
   - `MEMORY/` 是“相关会话记录”。

推荐完整指令：

```text
收工：按 TRACK.md 格式增量记录今天的调研产物。
```

## 落盘笔记

短指令：

```text
落盘笔记
```

执行口径：

1. 将会话中助手刚回答用户问题的上一条回答内容保存到 `notes/`。
2. 保存正文时保持原文不改变，不总结、不改写、不删减。
3. 可以在文件开头加入标题、记录时间和“以下为原文记录”这类外层说明。
4. 文件名使用记录日期时间和简短主题，例如：

```text
YYYY-MM-DD_HH-MM-SS_CST_主题.md
```

5. 只用于与股市、市场、股票、行业研究、行情数据源、研究工具或本项目方法论直接相关的回答；无关话题不落入本项目 `notes/`。

推荐完整指令：

```text
落盘笔记：把你上一条回答原文保存到 notes/。
```
