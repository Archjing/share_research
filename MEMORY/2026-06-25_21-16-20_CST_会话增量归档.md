# 会话增量归档

归档时间：2026-06-25_21-16-20_CST

起始点：接续 `2026-06-25_21-14-05_CST_会话原文归档.md` 的结束点。

说明：以下记录为上一次归档结束后新增的用户与助手可见消息原文。系统/开发者隐藏指令与工具内部输出未写入。

---

## USER

项目目录下创建MEMORY文件夹，把本次会话开始以来全部会话记录，不加删减，原文全量记录到这个文件夹下，文件名注归档日期时间。

## ASSISTANT

我会在当前项目目录创建 `MEMORY/`，把本轮对话中用户与助手可见消息按时间顺序归档。系统/开发者隐藏指令和工具内部输出不属于可见会话记录，我不会写入；正文会尽量保持原文，不做总结改写。

## ASSISTANT

我会先建目录，然后写一个 Markdown 归档文件。文件里会按 `USER` / `ASSISTANT` 标记消息边界，但每条消息正文保持原文。

## ASSISTANT

准备写入归档文件。由于内容较长，我会分块追加；这只是保存对话文本，不会改动现有研究文件。

## USER

创建PROMPTS.md 在其中记录上面这个“归档会话记录”任务，以便以后我简短地指令

## ASSISTANT

我会在项目根目录创建 `PROMPTS.md`，把“归档会话记录”的可复用短指令和完整执行口径写进去，方便你以后只说一句话触发同样操作。

## USER

归档会话记录从上一次的结束点增量归档。

## ASSISTANT

我会把这个增量规则写进 `PROMPTS.md`：以后触发“归档会话记录”时，不重复归档已有内容，而是从上一个归档文件结束处继续。与此同时，我会补一个增量归档文件，承接刚才那个归档文件的结束点。

## ASSISTANT

我会创建 `PROMPTS.md` 和一个新的增量归档文件，增量内容从上一个归档结束后的用户消息开始。
