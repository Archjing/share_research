# AGENTS.md

本文件面向 Codex、子代理和后续自动化工具，定义本项目的默认定位、工作规范和可复用研究工具。

## 项目定位

`share_research` 是一个长期反复进行的中国股市调查研究项目。

默认理解：

- 这是持续研究工作区，不是一次性回测目录。
- 个股、行业、指数样本、消息面、基本面和历史数据回看都是项目范围。
- 研究目标是沉淀可复用的 A 股研究方法论、候选池筛选框架和交易启发。
- 任何报告都应区分事实、推导、假设、启发和投资风险。

## 默认语言和风格

- 默认用中文写作。
- 面向用户的总结要直接、具体、可复用。
- 不把研究结论写成投资建议。
- 对不确定事实要明确标注来源、时间和口径。
- 涉及今天、最新、当前股价、公告、政策、成分股或财务数据时，必须重新核验，不沿用旧记忆当作当前事实。

## 目录职责

```text
reports/   正式调研报告，文件名前缀用三位序号
notes/     方法论、规律性知识、经验和阶段总结
MEMORY/    用户与助手可见会话的原文或增量归档
data/      原始输入数据、外部下载数据、行情快照
output/    脚本或分析过程生成的中间结果和统计表
tmp/       临时脚本、烟测输出、一次性检查结果和可丢弃中间产物
TRACK.md   长期调研轨迹索引
PROMPTS.md 可复用短指令
```

其中以下路径是软链接，指向相邻 `stok-mapping` 项目中的行情历史数据库：

```text
data/a_share  -> ../../stok-mapping/data/manual_history/a_share_history.sqlite  A 股行情历史数据库
data/hk_share -> ../../stok-mapping/data/hk_market_history.sqlite     港股行情历史数据库
data/us_share -> ../../stok-mapping/data/us_market_history.sqlite     美股行情历史数据库
```

根目录 `.env` 记录数据目录环境变量：

```text
A_SHARE_DATADIR=../stok-mapping/data/manual_history
EX_MARKET_DATADIR=../stok-mapping/data
TUSHARE_TOKEN_ENV=TUSHARE_TOKEN
TUSHARE_API_URL=http://api.tushare.pro
TUSHARE_TOKEN=<本地私有 token>
```

说明：`.env` 变量供脚本、Notebook 或 Agent 工具读取。Unix 软链接本身不会在路径解析时展开 `.env` 变量，因此软链接目标仍使用相对路径；若需要可配置的数据位置，应在脚本中读取环境变量，而不是依赖软链接自动展开变量。

根目录 `config.yaml` 记录在线数据源配置。Tushare 在线源参考相邻 `stok-mapping` 项目配置：

```yaml
data_sources:
  tushare:
    enabled: true
    token_env: TUSHARE_TOKEN
    api_url: http://api.tushare.pro
    request_delay: 0.25
    max_retries: 3
    retry_backoff: 2
    min_coverage: 0.80
```

Tushare token 从环境变量 `TUSHARE_TOKEN` 读取。本项目根目录 `.env` 可以作为本地私有配置保存 token，但不要把 token 明文写入报告、日志、会话归档或可共享材料。

不要把中间 CSV 写入 `reports/`。不要把正式报告写入 `output/`。会话归档只放入 `MEMORY/`。临时脚本、临时日志、一次性烟测结果和可丢弃文件放入 `tmp/`，不要混入长期数据目录。

## 工作前检查

开始任何较大研究任务前，先做四件事：

1. 查 `TRACK.md`，确认历史研究线索。
2. 查 `reports/` 和 `notes/`，复用已有方法论和结论。
3. 查 `MEMORY/`，理解用户之前的上下文和口径要求。
4. 查 `data/` 和 `output/`，确认现有数据是否可直接复用。

优先使用 `rg`、`rg --files`、`find`、`sed`、`head`、`tail` 做快速检查。

## 项目研究工具箱

### 工具 1：个股消息面跟踪框架

用于分析个股消息、订单、客户、政策或产业链事件。

固定链条：

```text
消息事件
-> 对应哪个业务？
-> 影响订单、价格、成本、产能还是估值？
-> 能否传导到收入？
-> 能否传导到利润率？
-> 市场是否已经充分预期？
-> 需要什么后续验证？
```

输出时至少包含：

- 业务板块。
- 消息来源。
- 证据等级。
- 传导逻辑。
- 后续验证点。
- 风险或反证。

### 工具 2：消息证据分级

默认分级：

| 等级 | 类型 | 使用方式 |
|---|---|---|
| A | 公司公告、财报、重大合同、中标公告、客户正式公告 | 可进入核心判断 |
| B | 行业数据、价格数据、政策文件、客户扩产、招投标信息 | 建立传导逻辑后使用 |
| C | 媒体报道、券商调研、产业链传闻、社交平台消息 | 作为观察线索 |
| D | 无来源传闻、概念炒作、模糊利好 | 只记录，不单独下结论 |

### 工具 3：行业变量表

不同类型公司优先跟踪不同变量：

| 类型 | 重点变量 |
|---|---|
| 周期品 | 产品价格、库存、开工率、供给收缩、价差 |
| 制造业 | 订单、招标、客户扩产、产能利用率、毛利率 |
| 消费品 | 批价、渠道库存、终端动销、提价能力、品牌势能 |
| 医药 | 临床、获批、医保、销售放量、海外授权 |
| 科技硬件 | 客户认证、新品导入、出货量、供应份额、国产替代 |
| 军工 | 型号、订单、交付、合同负债、采购节奏 |
| 平台型 | 用户、变现率、费用率、监管、生态位 |
| 资源品 | 商品价格、产量、成本、分红、汇率、政策 |

### 工具 4：数据口径核验清单

做收益、回撤、净值、估值或候选池分析时，必须说明：

- 样本来源。
- 起止日期。
- 价格口径：前复权、后复权、未复权或自建复权价。
- 权重口径：等权、市值加权、指数权重或动态权重。
- 样本口径：固定成分、动态成分、可交易样本或严格全样本。
- 缺失值处理。
- 是否存在幸存者偏差。
- 基准比较是绝对收益、差值超额，还是净值比值超额。

长期收益优先考虑稳定复权口径。若本地 `qfq` 或 `change_pct` 出现异常，应改用：

```text
未复权收盘价 × 复权因子
```

### 工具 5：候选股画像模板

分析一组候选股时，报告应覆盖：

- 样本来源和筛选规则。
- 行业或类型分布。
- 累计收益、CAGR、波动率、最大回撤。
- 市值、估值、换手率、成交额。
- 年度节奏和关键市场环境。
- 与基准指数的对照。
- 共性规律和例外样本。
- 幸存者偏差与不可外推之处。

### 工具 6：研究评分表草案

后续可将个股研究固化为评分表。默认维度：

| 维度 | 关注点 |
|---|---|
| 产业主线 | 是否处在能改变公司命运的长期变量中 |
| 公司地位 | 龙头地位、份额、壁垒、客户关系 |
| 消息传导 | 消息能否传导到收入、利润率或估值框架 |
| 估值位置 | 当前估值是否已经透支预期 |
| 资金强度 | 成交额、换手率、趋势和市场关注度 |
| 历史回撤 | 高弹性背后的持有压力 |
| 数据质量 | 数据来源、复权、样本和缺失处理是否可靠 |

## 报告格式建议

正式报告放在 `reports/`，文件名使用：

```text
NNN-主题.md
```

报告建议结构：

```text
# 标题

## 一句话结论
## 数据口径或资料来源
## 核心发现
## 分项分析
## 风险、反证和口径限制
## 后续跟踪点
## 输出文件
```

方法论总结放在 `notes/`，文件名建议使用：

```text
YYYY-MM-DD_HH-MM-SS_CST_主题.md
```

## 会话归档规范

当用户说“归档会话记录”时：

1. 使用 `MEMORY/`。
2. 从上一次归档文件结束点开始增量归档。
3. 只归档用户与助手可见消息原文。
4. 不写入系统隐藏指令、开发者隐藏指令、工具内部输出和环境上下文。
5. 每条消息用 `## USER` 或 `## ASSISTANT` 标记边界。

当用户说“收工”时：

1. 检查当日新增或更新的 `reports/`、`notes/`、`MEMORY/`。
2. 在 `TRACK.md` 中增量追加，不覆盖历史记录。
3. 记录主题、一句话摘要、报告链接、总结链接和会话归档链接。

## 当前阶段已知经验

- 个股消息面研究不能只判断“利好/利空”，要判断它是否改变收入、利润率、估值框架或市场预期差。
- 好股票往往不是孤立变好，而是踩中产业主线。
- 高收益样本通常伴随高波动和大回撤。
- 强势股常见路径是“业绩增长 + 估值重估”。
- 当前 AJ80 研究是成功样本归纳，不是严格策略验证。
- 回测和画像结论必须先通过数据口径校验。

## 禁止事项

- 不把历史回看写成确定性预测。
- 不把候选池写成买入清单。
- 不在未核验的情况下声称“最新”“当前”“今天”。
- 不删除或覆盖用户已有研究文件，除非用户明确要求。
- 不把不同口径的收益率混在一起比较。
